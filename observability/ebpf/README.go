//go:build ignore
// +build ignore

package main

// This directory contains eBPF programs (C files) that are compiled
// separately by the Makefile. The loader.go file should only be built
// when eBPF is actually needed.
